{
	"name": "racemanager",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest",
		"@types/express": "^5.0.1"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"scripts": {
		"build": "bun build . --compile --outfile dist/racemanager",
		"test": "bun test",
		"format": "bun run prettier --write .",
		"dev": "bun run --watch ."
	},
	"dependencies": {
		"discord.js": "^14.19.3",
		"dotenv": "^16.5.0",
		"express": "^5.1.0",
		"prettier": "^3.5.3"
	}
}
